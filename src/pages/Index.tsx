import PipelineCanvas from '../components/PipelineCanvas';

/**
 * Index page - Renders the full pipeline builder application.
 */
const Index = () => {
  return <PipelineCanvas />;
};

export default Index;
